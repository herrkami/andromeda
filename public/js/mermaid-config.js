// Mermaid configuration
mermaid.initialize({
  startOnLoad: false, // We'll manually control when mermaid runs
  securityLevel: 'loose',
  theme: 'neutral',
  flowchart: { 
    htmlLabels: true,
  },
});